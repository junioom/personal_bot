# Servidor HTTP (obrigatório para Render — substitui o run_polling)
flask==3.0.3

# Interface com o Telegram (v20+ assíncrona)
python-telegram-bot==20.7

# Google Sheets: autenticação e leitura
gspread==6.1.2
google-auth==2.29.0

# Manipulação dos dados da planilha
pandas==2.2.2

# Utilitário para carregar .env em desenvolvimento local
python-dotenv==1.0.1
